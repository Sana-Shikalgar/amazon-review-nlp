# Core data processing
pandas>=2.0.0
numpy>=1.23.0
pyarrow>=10.0.0
datasets>=2.10.0

# Machine learning
scikit-learn>=1.2.0
scipy>=1.10.0
statsmodels>=0.14.0
joblib>=1.3.0

# Deep learning (GPU build) and NLP
# Install a CUDA-enabled build for your GPU/CUDA version first, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then install the rest of this file. A CPU-only `pip install torch torchvision`
# also works but is far slower for the LSTM/BERT/multimodal notebooks.
torch>=2.4.0
torchvision>=0.19.0
transformers>=4.57.0

# Explainability
lime>=0.2.0.1
shap>=0.44.0

# Computer vision / image handling
pillow>=10.0.0
opencv-python>=4.8.0

# NLP utilities
nltk>=3.9.0
wordcloud>=1.9.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Data fetching / config
requests>=2.31.0
python-dotenv>=1.0.0

# Notebook execution
jupyter>=1.0.0
notebook>=7.0.0
nbconvert>=7.0.0
ipython>=8.0.0

# Utilities
tqdm>=4.65.0
